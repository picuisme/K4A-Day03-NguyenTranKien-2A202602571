# ==========================================
# DEPENDENCIES — Supply Chain Safe ReAct Agent (K4 Day 03)
# ==========================================

# Đọc file .env (bắt buộc)
python-dotenv>=1.0.0

# Google Gemini API — bắt buộc nếu LLM_PROVIDER=gemini (mặc định trong .env.example)
# SDK chính thức mới của Google (thay cho google-generativeai cũ đã deprecated)
#
# ⚠️ HÃY DÙNG BẢN MỚI NHẤT: 'pip install -U google-genai'
# Một số bản SDK cũ làm rớt trường `thought_signature` khi đọc phản hồi của Gemini 3,
# gây lỗi ở vòng lặp Tool Calling thứ 2:
#   400 INVALID_ARGUMENT: Function call is missing a thought_signature in functionCall parts
# (src/providers.py đã có lớp tự động thử lại để không gãy demo, nhưng bản SDK mới vẫn tốt hơn.)
google-genai>=0.5.0

# Các provider khác (chỉ cần nếu bạn đổi LLM_PROVIDER sang "openai" hoặc "anthropic")
# openai>=1.50.0
# anthropic>=0.40.0
